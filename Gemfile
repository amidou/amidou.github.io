source "https://rubygems.org"
gemspec

# ruby "3.2.4"

# gem "github-pages", group: :jekyll_plugins

# # Add jekyll-scholar to enable the `{% bibliography %}` tag used in `publications.md`.
# # Note: GitHub Pages does not support jekyll-scholar when building on github.com.
# # If you want GitHub to build the site, use GitHub Actions to build with this gem
# # or host the pre-built `_site/` output.
# gem "jekyll-scholar"
